<%= form_for(@fellowship) do |f| %>
  <% if @fellowship.errors.any? %>
    <div class="alert alert-block alert-danger">
      <h2><%= pluralize(@fellowship.errors.count, "error") %> prohibited this fellowship from being saved:</h2>

      <ul>
      <% @fellowship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <%= hidden_field_tag :affiliate_id, params[:affiliate_id] %>

  <div class="form-group">
    <%= f.label :specialty_id, "Specialty" %><br />
    <%#= f.number_field :speciality_id , :class => 'form-control' %>
    <%= collection_select(:fellowship, :specialty_id, Clinicalsection.all, :id, :sectionname, {:include_blank => '-- Choose Specialty --' , :selected =>  @fellowshipspecialty_id }  , :class=>'form-control') %>
  </div>

  <div class="form-group">
    <%= f.label :location %><br />
    <%= f.text_field :location ,:class => 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :training %><br />
    <%= f.text_field :training ,:class => 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
