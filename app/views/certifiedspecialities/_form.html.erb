<%= form_for @certifiedspeciality, :html => {:class => 'form'} do |f| %>
  <% if @certifiedspeciality.errors.any? %>
    <div class="alert alert-block alert-danger">
      <a class="close" aria-hidden="true" href="#" data-dismiss="alert">×</a>
      <h2><%= pluralize(@certifiedspeciality.errors.count, "error") %> prohibited this certified speciality from being saved:</h2>

      <ul>
      <% @certifiedspeciality.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag :affiliate_id, params[:affiliate_id] %>

  <div class="form-group">
    <%= f.label :speciality_id %><br />
    <%#= f.number_field :speciality_id , :class => 'form-control' %>
    <%= collection_select(:clinicalsection, :speciality_id, Clinicalsection.all, :id, :sectionname, {:include_blank => '-- Choose Specialty --'} , :class=>'form-control') %>
  </div>
  <div class="form-group">
    <%= f.label :year %><br />
    <%#= f.date_select :year, {:order => [:year], :start_year => 1900, :end_year => Time.now.year, :prompt => {:year => "Select year" }} , {:class => "form-control"} %>

    <%= f.select :year, ((1900..2014).map {|i| [i,i] }) , {:include_blank => '-- Choose Year --'} , {:class => "form-control"} %>
  </div>
  <div class="actions">
    <%= f.submit 'Add Specialty' %>
  </div>
<% end %>
